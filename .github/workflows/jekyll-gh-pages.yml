name: Jekyll with GitHub Pages dependencies preinstalled

on:
  workflow_dispatch:
  workflow_call:

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  # Build job
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout
        uses: actions/checkout@v3

      - name: copy GHP configs
        run: | 
          git config user.name 'github'
          git config user.email 'noreply@github.com'
          echo "base_pwd: $(basename $(pwd))"
          if [[ "$(basename $(pwd))" != "ghp" ]]; then
            echo "Not GHP, continue..."
            echo "git remote add ghp"
            git remote add -f ghp https://github.com/arafatm/ghp.git
            echo "git subtree add ghp"
            git subtree add --prefix ghp ghp main --squash
            echo "Before subtree fetch & pull ghp/Gemfile - $(head -n 5 ghp/Gemfile)"
            git fetch ghp main
            git subtree pull --prefix ghp ghp main --squash
            echo "After subtree fetch & pull ghp/Gemfile - $(head -n 5 ghp/Gemfile)"
            if [[ ! -d ".github/workflows" ]]; then mkdir -p .github/workflows; fi
            if [[ ! -f ".github/workflows/ghp.yml" ]]; then rm -f .github/workflows/*.yml; fi
            cp -u ghp/.github/workflows/ghp.yml .github/workflows/ghp.yml
            cp -ru ghp/_includes . && echo "copied ghp/_includes"
            cp -ru ghp/_layouts . && echo "copied ghp/_layouts"
            cp -u ghp/favicon.ico . && echo "copied ghp/favicon.ico"
            cp -u ghp/_config.yml . && echo "copied ghp/_config.yml"
            echo "git status: $(git status --short)"
            echo $(date) >> touch.txt
            if [[ -n $(git status --short) ]]; then
              git add .
              git commit -am "Git Action update"
              git push
              echo "hash: $(git rev-parse HEAD)"
            fi
            echo "--- touch.txt --- "
            tail -n 3 touch.txt
            echo 'Done copying ghp/'
          else
            echo "Is GHP..."
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v3

      - name: Build with Jekyll
        uses: actions/jekyll-build-pages@v1
        with:
          source: ./
          destination: ./_site
            
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v2

  # Deployment job
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v2
